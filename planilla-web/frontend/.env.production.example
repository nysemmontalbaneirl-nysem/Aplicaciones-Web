# Copia este archivo a ".env.production" (mismo nombre sin ".example") y
# pon la URL real del backend ya desplegado. Vite la toma automáticamente
# al correr "npm run build" — no hace falta ningún otro paso ni flag.
VITE_API_URL=https://planillas.grupojhcr.com/api
